<ul>
  <% if flash[:error] %>
  <% flash[:error].each do |error| %>
  <li><%= error %> </li>
  <% end %>
  <% end %>

</ul>

#####Need to add feature so only users can create bathrooms
<%= form_for @bathroom do |f| %>
<%= f.label :name %>
<%= f.text_field :name, placeholder: "Name of Bathroom" %>

<%= f.label :cleanliness %>
<%= f.text_field :cleanliness, placeholder: "Rate from 1-10" %><br>

<%= f.collection_check_boxes :location_ids, @locations, :id, :neighborhood %><br>
<%= f.fields_for :locations, @bathroom.locations.build do |locations_fields| %>
    <%= locations_fields.text_field :neighborhood, placeholder: "Write a Neighborhood" %>
  <% end %>
<%= f.submit %>

<% end %>
