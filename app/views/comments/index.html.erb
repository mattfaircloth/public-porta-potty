<h3>Bathroom Comments:</h3>
<ul>
  <% @comments.each do |comment| %>
  <li> <%= link_to comment.bathroom.name, comment.bathroom, class: 'btn btn-info' %> - <%= link_to comment.content, comment, class: 'btn btn-success' %></li>
  <% end %>
</ul>
